# pieces-decidim

This library was generated with [Nx](https://nx.dev).

## Building

Run `npx nx build pieces-decidim` to build the library.

## Running unit tests

Run `npx nx test pieces-decidim` to execute the unit tests via [Jest](https://jestjs.io).
